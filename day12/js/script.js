// Scope block 

var car = '' /* Variable definition in global scope which will later receive the value of the variable through the function and print it correctly*/

if (ture) {
    var car = 'Tesla'
}

console.log(car)